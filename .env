# Connection to PostgreSQL installed directly on this laptop. Replace the
# placeholders with your local PostgreSQL credentials before starting the app.
DATABASE_URL=postgres://reconciliation:strongpassword@localhost:5432/Reconciliation
RECON_WEB_ADDR=127.0.0.1:3000
